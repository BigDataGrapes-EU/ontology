CONSTRUCT {
    ?URI a bdg:PlotCluster ;
        skos:broader ?BROADER ;
        rdfs:label   ?LABEL ;
        bdg:idGeocledian ?id_geocledian .
}
WHERE {
    bind(uri(concat(?broader,"/",str(?uri))) as ?URI)
    bind(uri(?broader) as ?BROADER)
    bind(if(bound(?label),?label,?uri) as ?LABEL)
}